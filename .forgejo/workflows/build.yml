name: Build and Push

on:
  push:
    branches: [main]
    paths: ['docker/**', 'VERSION', 'KEYS']

jobs:
  build:
    runs-on: docker
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Read VERSION
        id: version
        run: echo "version=$(cat VERSION | tr -d '[:space:]')" >> "$GITHUB_OUTPUT"

      - name: Read KEYS
        id: keys
        run: echo "keys=$(tr '\n' ' ' < KEYS)" >> "$GITHUB_OUTPUT"

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ secrets.GHCR_USERNAME }}
          password: ${{ secrets.GHCR_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: ./docker
          platforms: linux/amd64,linux/arm64
          push: true
          build-args: |
            VERSION=${{ steps.version.outputs.version }}
            KEYS=${{ steps.keys.outputs.keys }}
          tags: |
            ghcr.io/kub0-ai/monero:${{ steps.version.outputs.version }}
            ghcr.io/kub0-ai/monero:latest
          cache-from: type=registry,ref=ghcr.io/kub0-ai/monero:buildcache
          cache-to: type=registry,ref=ghcr.io/kub0-ai/monero:buildcache,mode=max
