name: Backfill Version

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Monero version (e.g., 0.18.4.5)'
        required: true
      keys:
        description: 'GPG keys (space-separated, overrides KEYS file)'
        required: false

permissions:
  contents: write
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Resolve KEYS
        id: keys
        run: |
          if [ -n "${{ github.event.inputs.keys }}" ]; then
            echo "keys=${{ github.event.inputs.keys }}" >> "$GITHUB_OUTPUT"
          else
            echo "keys=$(tr '\n' ' ' < KEYS)" >> "$GITHUB_OUTPUT"
          fi

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: ./docker
          platforms: linux/amd64,linux/arm64
          push: true
          build-args: |
            VERSION=${{ github.event.inputs.version }}
            KEYS=${{ steps.keys.outputs.keys }}
          tags: ghcr.io/kub0-ai/monero:${{ github.event.inputs.version }}
          cache-from: type=registry,ref=ghcr.io/kub0-ai/monero:buildcache
          cache-to: type=registry,ref=ghcr.io/kub0-ai/monero:buildcache,mode=max

      - name: Create git tag
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git tag -a "v${{ github.event.inputs.version }}" -m "Monero ${{ github.event.inputs.version }}" || true
          git push origin "v${{ github.event.inputs.version }}" || true
